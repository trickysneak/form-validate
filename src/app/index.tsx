import React from 'react';
import { Container } from '../components/container';
import { Form } from '../features/register-form/form';

export const App = () => {
  return (
    <Container>
      <Form />
    </Container>
  );
};
